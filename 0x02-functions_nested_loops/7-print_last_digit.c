#include "main.h"
#include <ctype.h>

/**
 * print_last_digit print last digit on one number
 *
 * Return: always valus (sucess)
 * @n : is parameter
 */

int print_last_digit(int n)

{
	return (n % 10);
}
