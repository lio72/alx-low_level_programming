#include "main.h"
#include <stdio.h>

/**
 * nul - fuction test if enter is digit
 *
 * @a : a is parameter1
 * @b : b is parameter2
 * Return: always 0 (sucess)
 */

int mul(int a, int b)
{
	val = a * b;
	return (val);

}
