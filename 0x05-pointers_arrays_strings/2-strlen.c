#include "main.h"

/**
 * _strlen - is function that give len
 * strlen - is function to use
 * @s : is parameter
 * Return : length of string
 */

int _strlen(char *s)

{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}

	return (i);
}
