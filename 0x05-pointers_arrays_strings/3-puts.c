#include "main.h"

/**
 * _strlen - is function that return the length of string
 * strlen - is function to use
 * @s : is parameter
 * Return : length of string
 */
int _strlen(char *s)
{
	int i = 0;

	for (i = 0; s[i] != '\0'; i++)
	{

	}

	return (i);
}
