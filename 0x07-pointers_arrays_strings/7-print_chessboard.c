#include "main.h"

/**
* print_chessboard - function return firt occurence of @c.
* @a: is parameter1.
*
* Return: pointer to destination string.
*/

void print_chessboard(char (*a)[8])

{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		if (s[i] == c)
		{
			return (s + i);
		}

	}
	return (NULL);

}
