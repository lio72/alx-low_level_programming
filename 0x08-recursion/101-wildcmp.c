#include "main.h"
#include <string.h>

/**
* wildcmp - function that concatenates two strings.
* @s1: pointer to destination string.
* @s2: pointer2
*
* Return: pointer to destination string.
*/

int wildcmp(char *s1, char *s2)

{
	if (*s == '\0')
	{
		return (0);
	}

	return (strlen(s));
}
